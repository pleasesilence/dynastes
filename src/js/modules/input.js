function input() {
    if (document.querySelector('form__input')) {
        const input = document.querySelectorAll('form__input'),
              button = document.querySelectorAll('controls_confirm');
        
    }
}

module.exports = input;